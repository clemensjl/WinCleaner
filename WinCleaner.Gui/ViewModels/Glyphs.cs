namespace WinCleaner.Gui.ViewModels;

/// <summary>
/// Icon-Glyphen aus "Segoe Fluent Icons" (Codepoints als \u-Escape, damit der
/// Quelltext reiner ASCII bleibt). Namen beschreiben das Symbol.
/// </summary>
internal static class Glyphs
{
    public const string Home     = ""; // Übersicht (Home)
    public const string Delete   = ""; // Aufräumen (Delete)
    public const string Storage  = ""; // Speicher (DataSense/Storage)
    public const string Apps     = ""; // Programme (AllApps)
    public const string Settings = ""; // Autostart & Dienste (Settings)
    public const string Shield   = ""; // Privatsphäre (Shield)
    public const string Warning  = ""; // Sicher löschen (Warning)
    public const string Restore  = ""; // System (UpdateRestore)
}
