<UserControl x:Class="WinCleaner.Gui.Views.StartupServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,24,28,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock Text="Autostart &amp; Dienste" Style="{StaticResource H1}" />
                <TextBlock Text="Autostart-Einträge reversibel deaktivieren. Dienste über ein kuratiertes, sicheres Profil."
                           Style="{StaticResource Muted}" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Content="Sicheres Dienste-Profil" Style="{StaticResource SecondaryButton}"
                        Command="{Binding SafeServicesCommand}" Margin="0,0,8,0" />
                <Button Content="Ausgewählten deaktivieren" Style="{StaticResource PrimaryButton}"
                        Command="{Binding DisableCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="6" Margin="0,16,0,0">
            <ListBox ItemsSource="{Binding Items}" SelectedItem="{Binding Selected, Mode=TwoWay}"
                     Background="Transparent" BorderThickness="0"
                     ItemContainerStyle="{StaticResource Row}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.4*" />
                                <ColumnDefinition Width="2.4*" />
                                <ColumnDefinition Width="170" />
                                <ColumnDefinition Width="60" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" Style="{StaticResource Body}"
                                       TextTrimming="CharacterEllipsis" Margin="0,0,8,0" />
                            <TextBlock Grid.Column="1" Text="{Binding Path}" Style="{StaticResource Muted}"
                                       TextTrimming="CharacterEllipsis" Margin="0,0,8,0" />
                            <TextBlock Grid.Column="2" Text="{Binding Source}" Style="{StaticResource Muted}"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="3" Text="{Binding StateText}" HorizontalAlignment="Right"
                                       Foreground="{DynamicResource TextPrimary}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
